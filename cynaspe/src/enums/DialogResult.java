package enums;

// What the user choose when closing a dialog
public enum DialogResult {
    OK, // The user clicked OK
    CANCEL, // The user clicked CANCEL
    CLOSED // The user exited the dialog
}